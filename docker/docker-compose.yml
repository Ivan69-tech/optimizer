services:

  # ── Service d'optimisation (API REST) ───────────────────────────────────────
  optimizer-service:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
    network_mode: host