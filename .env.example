# === Base de données ===
DATABASE_URL=postgresql://optimizer:optimizer@localhost:5432/optimizer

# === Authentification API ===
# Map JSON site_id -> api_key. Une clé par site.
SITE_API_KEYS={"site-test-01":"change_me_xxx"}

# === Logging ===
LOG_LEVEL=INFO

# === Configuration fonctionnelle (fichier YAML) ===
CONFIG_PATH=config.yaml
